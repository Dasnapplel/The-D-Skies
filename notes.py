    # pygame has music that plays and is held in memory, so it can be played in the background
    # pygame has collision detection with the mouse
    # pygame has collision detection with rectangles, circles, and polygons
    # to do gravity, have a constant y velocity, and have it stop whn it collides with a screen object